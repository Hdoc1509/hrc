import { RippleSpinner } from "@lib/main";

export function RippleSpinnerSizes() {
  return (
    <>
      <RippleSpinner color="error" size="small" />
      <RippleSpinner color="error" />
      <RippleSpinner color="error" size="large" />
    </>
  );
}

