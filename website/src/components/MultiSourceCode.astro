---
import { Code, Tabs, TabItem } from "@astrojs/starlight/components"

interface Props {
  sources: {
    code: string,
    label: string
  }[]
}

const { sources } = Astro.props
---

<details>
  <summary>Show code</summary>

  <Tabs>
    {sources.map(source => (
      <TabItem label={source.label}>
        <Code code={source.code} lang="tsx" />
      </TabItem>
    ))}
  </Tabs>
</details>

<style>
  summary {
    cursor: pointer;
    user-select: none;
  }
</style>
